 echo "Factorial Program"
echo "Enter the number of which you want to find the factorial:"
read num;
fact=1;
for ((i = 1 ; i <= num; i+=1)); do
	fact=$((fact * i))
done
echo "the factorial of number: $num is  $fact"
	
